% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_table_shell.R
\name{make_table_shell}
\alias{make_table_shell}
\title{Generate a Programmatic Survey Table Shell}
\usage{
make_table_shell(
  data,
  by_list,
  col_spec,
  title = NULL,
  engine = c("gt", "flextable"),
  show_group_labels = FALSE,
  hide_first_group = FALSE,
  stripe_color = "#dbe7f3",
  group_color = "#dbe7f3"
)
}
\arguments{
\item{data}{A data frame containing labelled variables (via the \code{labelled} package).
Only metadata (variable labels and value labels) is used.}

\item{by_list}{A named list defining row structure. Each element represents a row block:
\itemize{
\item \code{NULL}: creates a single row with label from the element name
\item \code{"var_name"}: creates one row per value label of \code{var_name}
\item All other formats (vectors, etc.) produce an informative error.
}}

\item{col_spec}{A list of column block specifications. Each element is a list with:
\itemize{
\item \code{type}: \code{"categorical"} or \code{"numeric"}
\item \code{var}: variable name in data
\item \code{label}: (numeric only) display label for the column
}}

\item{title}{Optional character string for table title.}

\item{engine}{\code{"gt"} or \code{"flextable"} (default: \code{gt}). Controls output table class.}

\item{show_group_labels}{Logical (default \code{FALSE}). Show group header rows.}

\item{hide_first_group}{Logical (default \code{FALSE}). Suppress header row for first group.}

\item{stripe_color}{Hex color for alternating row stripes (default: \code{"#dbe7f3"}).}

\item{group_color}{Hex color for group header rows (default: \code{"#dbe7f3"}).}
}
\value{
\itemize{
\item If \code{engine = "gt"}: a \code{gt_tbl} object
\item If \code{engine = "flextable"}: a \code{flextable} object
}
}
\description{
Constructs an empty table structure (shell) with correct labels, groupings, and
column headers from labelled data. The shell contains no computed values, making it
suitable for sharing with stakeholders for review.
}
\examples{
\dontrun{
# Create sample labelled data
library(labelled)
data <- tibble::tibble(
  admin1 = labelled(
    c(1, 2, 1, 2),
    c(North = 1, South = 2)
  ),
  urb_rur = labelled(
    c(1, 1, 2, 2),
    c(Urban = 1, Rural = 2)
  ),
  cookstove_type = labelled(
    c(1, 2, 1, 3),
    c(Wood = 1, Gas = 2, Electric = 3)
  )
)
var_label(data$admin1) <- "Zone"
var_label(data$urb_rur) <- "Settlement"
var_label(data$cookstove_type) <- "Cookstove type"

# Create table shell
make_table_shell(
  data = data,
  by_list = list(
    "Nigeria" = NULL,
    "Zone" = "admin1"
  ),
  col_spec = list(
    list(type = "categorical", var = "cookstove_type")
  ),
  title = "Primary cookstove type by zone",
  engine = "gt"
)
}

}
